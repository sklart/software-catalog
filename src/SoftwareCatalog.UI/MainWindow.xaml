<Window x:Class="SoftwareCatalog.UI.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Software Catalog" Width="900" Height="600"><Grid Margin="12"><Grid.RowDefinitions><RowDefinition Height="2*"/><RowDefinition Height="3*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><GroupBox Header="Scan folders"><DockPanel><StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0"><Button Content="Add Folder" Command="{Binding AddFolderCommand}"/><Button Content="Remove" Command="{Binding RemoveFolderCommand}" Margin="8,0,0,0"/><Button Content="Scan" Command="{Binding ScanCommand}" Margin="8,0,0,0"/><Button Content="Cancel" Command="{Binding CancelCommand}" Margin="8,0,0,0"/></StackPanel><ListBox ItemsSource="{Binding ScanRoots}" SelectedItem="{Binding SelectedScanRoot}" DisplayMemberPath="StoredPath"/></DockPanel></GroupBox><GroupBox Grid.Row="1" Header="Files"><DataGrid ItemsSource="{Binding Files}" AutoGenerateColumns="False" IsReadOnly="True"><DataGrid.Columns><DataGridTextColumn Header="Name" Binding="{Binding FileName}"/><DataGridTextColumn Header="Path" Binding="{Binding RelativePath}"/><DataGridTextColumn Header="Size" Binding="{Binding Size}"/><DataGridTextColumn Header="Modified" Binding="{Binding LastWriteTimeUtc}"/><DataGridCheckBoxColumn Header="Exists" Binding="{Binding Exists}"/></DataGrid.Columns></DataGrid></GroupBox><TextBlock Grid.Row="2" Text="{Binding StatusText}" Margin="0,8,0,0"/></Grid></Window>
